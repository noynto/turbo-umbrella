---
apiVersion: v1
kind: Service
metadata:
  name: turbo-umbrella
  labels:
    app: turbo-umbrella
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: turbo-umbrella
---